# Streamlit secrets (copy to secrets.toml and fill in)
# In Streamlit Cloud, set these via the Secrets UI

app_password = "change-this-in-production"
allowed_email_domains = ["goodpatch.com", "anywhere.goodpatch.com"]
